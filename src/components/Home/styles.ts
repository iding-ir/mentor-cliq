import { Styles } from "../../types";

export const styles: Styles = {
  Home: {
    margin: "6rem auto",
    textAlign: "center",
    fontSize: "6rem",
    color: (theme) => theme.palette.grey[500],
  },
};
