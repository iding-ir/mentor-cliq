import { Styles } from "../../types";

export const styles: Styles = {
  wrapper: {
    display: "flex",
    flexDirection: "column",
    width: "500px",
    height: "100%",
    margin: "0 auto",
    justifyContent: "center",
  },
};
