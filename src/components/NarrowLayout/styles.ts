import { Styles } from "../../types";

export const styles: Styles = {
  wrapper: {
    display: "flex",
    flexDirection: "column",
    height: "100%",
    margin: "0 auto",
    justifyContent: "center",
  },
};
